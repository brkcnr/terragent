# TerrAgent Environment Configuration Example
# Copy this file to .env and insert your API key if using Gemini features in later milestones.
# NEVER commit your actual .env file with real API keys to version control.

GEMINI_API_KEY=your_gemini_api_key_here
