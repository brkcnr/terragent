<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>TerrAgentBridge</RootNamespace>
    <AssemblyName>TerrAgentBridge</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Standard .NET references for JSON and WebSockets are built into net8.0 -->
  </ItemGroup>
</Project>
